// 这里暴露出所有共用的属性以及方法

// iconfont
import { setIconFontFunc } from './iconfont-common'
// 工具函数
import { utilListMethod } from './util'
export const setIconFont = setIconFontFunc
export const utilList = utilListMethod
